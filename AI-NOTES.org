#+TITLE: AI Notes

* 2026-05-20 Manueller Vergleich

** Änderungen

- DWDS:
  - Qualifier aus =dwdswb-diasystematik= werden breiter erkannt, damit Fälle wie =Glücksspiel= oder regionale Marker nicht verloren gehen.
  - Verweisdefinitionen aus =dwdswb-verweise= werden als Definition übernommen, z. B. =Synonym zu Bankhalter = ...=.
  - Leerzeichen innerhalb von Winkelklammern werden bereinigt, z. B. =⟨ frei Haus ⟩= -> =⟨frei Haus⟩=.
  - Mehrwortausdruck-Fälle werden gezielt für die im manuellen Vergleich beschriebenen Strukturen als =... (MWA) = ...= ausgegeben.
- Duden:
  - Führende =Kurzform für=-Tuple werden als Definition behandelt statt als Qualifier.
  - Breite Winkelklammern =〈〉= werden in den geparsten Texten zu =⟨⟩= normalisiert.
- Wiktionary:
  - Redewendungen berücksichtigen nun auch Einträge ohne vorangestellte Sense-Nummer.
  - Bei Redewendungen wird nur der eigentliche Ausdruck übernommen, nicht die nachgestellte Gloss-Erklärung.
- Dokumentation:
  - README und Wiktionary-Spezifikation dokumentieren nun, dass unsicher markierte Wiktionary-Qualifier konservativ in der Definition verbleiben.

** Checks

- Batch-Parser-Checks lokal gegen Fixtures:
  - DWDS =Bank=: inhaltlich geprüft
  - DWDS =Zaun=: inhaltlich geprüft
  - Duden =Bank=: inhaltlich geprüft
- Zusätzliche gezielte Parser-Checks:
  - =buttercup= ist lokal nicht im Load-Path verfügbar, deshalb kein direkter Testlauf über die Buttercup-Suiten.
  - Die neuen Duden/Wiktionary-Fälle wurden stattdessen über gezielte Batch-Parser-Checks und Testanpassungen abgesichert.
- =eask test buttercup= konnte in dieser Umgebung nicht genutzt werden, weil =~/.eask= nicht beschreibbar ist.

** Export / HTML

- Neuer Exportordner angelegt: =tmp/woerterbuch-export-2026-05-20/=
- Aktuelle HTML-Snapshots gespeichert für:
  - DWDS: =Bank=, =Haus=, =Zaun=, =springt=, =Wolke=, =verlieben=
  - Duden: =Bank= (beide Homographe), =Haus=, =Zaun=, =springen=, =Wolke=, =verlieben=
  - Wiktionary: =Bank=, =Haus=, =Zaun=, =springen=, =Wolke=, =verlieben=
- Ein neuer Ergebnisexport wurde begonnen; aktuell liegt mindestens =bank.el= im neuen Exportordner.

** Offene Punkte

- Die Fixture-Dateien für DWDS =Haus= und teils =Bank= sollten noch sauber nachgezogen oder mit einem vollständigen lokalen Testlauf bestätigt werden; die Logikänderungen selbst sind eingegrenzt, aber der vollautomatische Gleichlauf aller erwarteten Einzeiler wurde in dieser Session nicht vollständig abgeschlossen.

* 2026-05-21 Testing mit Fixtures

** Änderungen

- Die Source-Tests für =duden=, =dwds=, =openthesaurus= und =wiktionary= wurden stark vereinfacht.
- Die bisherigen parsernahen Einzeltests wurden durch reine Fixture-Vergleiche ersetzt:
  - gespeicherte HTML/JSON-Antworten werden als Mock für =url-retrieve= verwendet
  - pro Source, Wort und Section wird nur noch der öffentliche Fetch-Weg gegen den gespeicherten erwarteten Output geprüft
- Gemeinsame Test-Helfer und die URL-zu-Fixture-Zuordnung liegen nun zentral in =tests/test-helper.el=.
- Spezielle 404-Fälle ohne gespeicherten Body werden im Mock deterministisch simuliert:
  - Duden =Bank= initial 404 mit Search-Fallback
  - Duden =Nixdaexistiert=
  - Wiktionary =Nixdaexistiert=

** Checks

- Vollständiger Fixture-Abgleich aller Kombinationen aus Source, Wort und Section per Batch-Emacs:
  - =emacs -Q --batch -L lisp -L tests -l test-helper --eval ...=
  - Ergebnis: alle Vergleiche erfolgreich
- =eask test buttercup= konnte in dieser Umgebung nicht direkt ausgeführt werden:
  - Standardpfad =~/.eask= ist read-only
  - mit lokalem =HOME= im Repo scheitert die Installation von =buttercup= am gesperrten Netzwerkzugriff

* 2026-05-21 Testhelper-Fix

** Änderungen

- In =tests/test-woerterbuch.el= wurde die interaktive Hilfsfunktion zum Generieren des erwarteten Outputs in =test-helper-woerterbuch-generate-expected-output= umbenannt.
- Der Sammelaufruf =test-helper-woerterbuch-update-output= verwendet nun den neuen Namen.
- Damit überschreibt die Datei nicht mehr die 3-Argument-Funktion =test-helper-woerterbuch-fetch-expected-output= aus =tests/test-helper.el=.

** Checks

- =make test=
  - Ergebnis: 140 Specs, 0 Fehler

* 2026-05-21 Checkdoc-Warnungen

** Änderungen

- In den Backend-Dateien wurden fehlende =;;; Commentary:= und =;;; Code:=-Abschnitte ergänzt.
- Betroffene Docstrings wurden so präzisiert, dass die Parameter =INPUT=, =LEMMA=, =DOM= und =SECTIONS= explizit genannt werden.
- Die Timeout-Fehlermeldung in =woerterbuch-core.el= beginnt nun mit einem Großbuchstaben, damit sie Checkdoc entspricht.

** Checks

- Batch-Checkdoc lokal für:
  - =lisp/woerterbuch-core.el=
  - =lisp/woerterbuch-duden.el=
  - =lisp/woerterbuch-dwds.el=
  - =lisp/woerterbuch-openthesaurus.el=
  - =lisp/woerterbuch-wiktionary.el=
- =make test=
  - Ergebnis: 140 Specs, 0 Fehler
